# Deterministic ADLC gates. Copied repositories replace the marked placeholder
# with their measured product commands during project onboarding.
name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  quality:
    name: quality
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # CUSTOMIZE: replace this copied-template placeholder with the measured
      # build, lint, test, and environment gates recorded by $project-onboarding.
      # Keep scripts/tuning-status.sh until the license trial is accepted.
      - name: Show scaffold onboarding status
        run: scripts/tuning-status.sh --ci

  scaffold-self-check:
    name: scaffold-self-check
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: read
    steps:
      - name: Check out the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Check whitespace and shell syntax
        env:
          DIFF_BASE: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: |
          if [[ -n "$DIFF_BASE" && ! "$DIFF_BASE" =~ ^0+$ ]]; then
            git diff --check "$DIFF_BASE" "$GITHUB_SHA"
          else
            git show --check --format= "$GITHUB_SHA"
          fi
          bash -n scripts/*.sh .agents/skills/plan-management/scripts/*.sh

      - name: Check shell scripts with ShellCheck
        env:
          SHELLCHECK_VERSION: 0.11.0
          SHELLCHECK_SHA256: 8c3be12b05d5c177a04c29e3c78ce89ac86f1595681cab149b65b97c4e227198
        run: |
          archive="$RUNNER_TEMP/shellcheck.tar.xz"
          curl --fail --silent --show-error --location --proto '=https' --tlsv1.2 \
            "https://github.com/koalaman/shellcheck/releases/download/v${SHELLCHECK_VERSION}/shellcheck-v${SHELLCHECK_VERSION}.linux.x86_64.tar.xz" \
            --output "$archive"
          printf '%s  %s\n' "$SHELLCHECK_SHA256" "$archive" | sha256sum --check --strict
          tar -xJf "$archive" -C "$RUNNER_TEMP"
          git ls-files -z '*.sh' \
            | xargs -0 "$RUNNER_TEMP/shellcheck-v${SHELLCHECK_VERSION}/shellcheck" -S warning

      - name: Validate GitHub workflows with actionlint
        env:
          ACTIONLINT_VERSION: 1.7.12
          ACTIONLINT_SHA256: 8aca8db96f1b94770f1b0d72b6dddcb1ebb8123cb3712530b08cc387b349a3d8
        run: |
          archive="$RUNNER_TEMP/actionlint.tar.gz"
          curl --fail --silent --show-error --location --proto '=https' --tlsv1.2 \
            "https://github.com/rhysd/actionlint/releases/download/v${ACTIONLINT_VERSION}/actionlint_${ACTIONLINT_VERSION}_linux_amd64.tar.gz" \
            --output "$archive"
          printf '%s  %s\n' "$ACTIONLINT_SHA256" "$archive" | sha256sum --check --strict
          tar -xzf "$archive" -C "$RUNNER_TEMP" actionlint
          "$RUNNER_TEMP/actionlint" -color

      - name: Check Markdown links
        run: scripts/check-md-links.sh

      - name: Check Issue form and body-template synchronization
        run: scripts/check-template-sync.sh

      - name: Check Codex skills and scaffold policy
        run: scripts/check-skills.sh

      - name: Check third-party action pins
        run: |
          references="$(grep -RhE '^[[:space:]]*uses:' .github/workflows \
            | sed -E 's/^[[:space:]]*uses:[[:space:]]*//; s/[[:space:]]+#.*$//')"
          while IFS= read -r reference; do
            [[ -n "$reference" ]] || continue
            [[ "$reference" == ./* ]] && continue
            if [[ ! "$reference" =~ ^[^@[:space:]]+@[0-9a-f]{40}$ ]]; then
              echo "Action reference is not a full commit SHA: $reference" >&2
              exit 1
            fi
          done <<< "$references"

      - name: Build the read-only retro report
        env:
          GH_TOKEN: ${{ github.token }}
        run: scripts/retro-hygiene.sh -R "$GITHUB_REPOSITORY"
